# ─────────────────────────────────────────────────────────────────────────────
# AeroClean — Python dependencies
#
# Install on Raspberry Pi OS Trixie (Python 3.12+):
#   pip install -r requirements.txt
#
# See README.md Setup section for full Pi setup instructions.
# ─────────────────────────────────────────────────────────────────────────────

# Core computer vision
opencv-python>=4.9.0
numpy>=1.26.0

# Pi camera (IMX708) — pip-installable; requires libcamera system libraries on Pi
picamera2>=0.3.0

# OCR (Model 1)
pytesseract>=0.3.10
# Also install the Tesseract binary:
#   Raspberry Pi / Debian:  sudo apt install tesseract-ocr
#   macOS:                  brew install tesseract
#   Windows:                https://github.com/UB-Mannheim/tesseract/wiki

# Object detection (Model 2)
ultralytics>=8.3.0

# MAVLink / drone mission (mission mode only)
pymavlink>=2.4.41
# RPi.GPIO is Pi-only — install manually on the Pi:
#   pip install RPi.GPIO

# VL53L3CX ToF range sensor — Sensor B (I2C, alternative)
# Required when range_sensor.type = "b" in config.json.
adafruit-circuitpython-vl53l4cd>=1.3.0
adafruit-blinka>=8.0.0
# Both packages require hardware I2C on the Pi — gracefully no-op on non-Pi systems
# adafruit-blinka also depends on RPi.GPIO — install it on the Pi:
#   pip install RPi.GPIO

# TF-Luna / TFMini UART range sensor — Sensor A (UART, default)
# Required when range_sensor.type = "a" in config.json (the default).
# Used by sensors.py (TFRangeSensor), sensor_ocr_test.py, and sensor_tf_test.py.
# Gracefully skipped at runtime if pyserial is absent and sensor B is selected.
pyserial>=3.5
